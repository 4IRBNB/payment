server:
  port: 19096
  address: 0.0.0.0

eureka:
  client:
    service-url:
      defaultZone: http://43.203.174.245:19090/eureka/

spring:
  application:
    name: payment
  datasource:
    initialize: true
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: team4irbnb
    url: jdbc:postgresql://team26.cxqqm2i6ohrd.ap-northeast-2.rds.amazonaws.com:5432/postgres?currentSchema=payments
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect  # PostgreSQL Dialect
    properties:
      hibernate:
        default_schema: payments
  data:
    redis:
      host: team26redis-3bjj75.serverless.apn2.cache.amazonaws.com
      port: 6379

common:
  jpa:
    enabled: true